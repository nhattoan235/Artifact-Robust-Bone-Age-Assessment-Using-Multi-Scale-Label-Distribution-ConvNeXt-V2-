# Workspace rules

- Never push commits, branches, tags, or other changes to any remote repository unless the user explicitly authorizes that specific push.
- Read-only remote operations such as `git fetch`, `git ls-remote`, and inspecting remote commits are allowed when needed for the task.
